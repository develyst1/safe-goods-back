# Local-only configuration (SPEC-001 §Non-functional). Copy to `.env` — `.env` is git-ignored.
# There is no real environment; these placeholders are NOT secrets.
PORT=3001
DATABASE_PATH=data/safe-goods.sqlite
UPLOAD_DIR=uploads
JWT_SECRET=local-dev-secret-change-me
AUTO_RELEASE_SECONDS=259200
SWEEP_INTERVAL_MS=60000
CORS_ORIGIN=http://localhost:3000
# Seeded admin (`bun run seed`)
ADMIN_EMAIL=admin@local.test
ADMIN_PASSWORD=admin1234
ADMIN_DISPLAY_NAME=Admin
