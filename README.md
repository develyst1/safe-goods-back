# safe-goods-back

Bun + Hono API for **เว็บกลาง** (safe-goods). Local only.

1. `cp .env.example .env`
2. `bun install`
3. `bun run db:migrate`
4. `bun run seed`
5. `bun run dev` → http://localhost:3001/api/v1/health
